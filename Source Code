#include<stdio.h>
int sum(int n);

int main()
{
int num;

scanf("%d",&num);
   sum(num);

    return 0;
}

int sum(int n)
{
    int rem,sum=0;

    while(n>0)
    {
      rem=n%10;
      sum=sum+rem;
      n=n/10;
    }
    printf("The Sum is : %d",sum);
}
